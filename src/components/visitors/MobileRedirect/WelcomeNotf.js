import React from 'react';
import { Card } from 'reactstrap';

export default function WelcomeNotf(props) {

    // setTimeout(props.history.push(`/`), 5000)

  return <div>
      <Card>
          <div>
              Welcome
              Your Host Has Been Notified
          </div>
      </Card>

  </div>;
}
